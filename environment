PGPORT="${DB_PORT:=$PGPORT}"
PGHOST="${DB_HOST:=$PGHOST}"
PGUSER="${DB_USER:=$PGUSER}"
PGPASSWORD="${DB_PASS:=$PGPASSWORD}"
PGDATABASE="${DB_NAME:=$PGDATABASE}"
DATABASE_URL="${DATABASE_URL:=ecto://$PGUSER:$PGPASSWORD@$PGHOST:$PGPORT/$PGDATABASE}"
MIX_ENV="${MIX_ENV}"
